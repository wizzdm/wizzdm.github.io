language: ruby
rvm:
 - 2.2
 - jruby
 - rbx-3

sudo apt-get install ruby1.9.1-dev
sudo gem install travis

# via ruby
travis encrypt -r wizzdm/wizzdm.github.io GITHUB_TOKEN=05cd854bdf1aaae4b4b2804c3410976eb91e4ed6

branches:
  only:
  - master
language: python
python:
- 3.5
install:
- pip install -r requirements.txt
script:
- make html
notifications:
  email:
    on_success: always
    on_failure: always

before_install:
  - git submodule update --init --recursive
after_success: bash deploy.sh
